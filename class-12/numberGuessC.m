function [ans_num, guesses] = numberGuessC(min, max, count)
  % Guessing game with a guess counter.
  % min, max  - current search range (for example, call with 1, 100 to start)
  % count     - number of guesses so far (always call with 0 to start)
  %
  % Returns [ans_num, guesses] — the found number and total guesses used.
  %
  % Hint: the structure mirrors numberGuess.m; you only need to
  %   (a) increment count each time a guess is made, and
  %   (b) pass count along in the recursive calls.

  % YOUR CODE HERE

end
