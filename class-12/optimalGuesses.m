function g = optimalGuesses(n)
  % Returns the minimum number of binary-search guesses needed
  % for a range containing n integers.
  %
  % Think recursively:
  %   - If there is only 1 possibility, you need 0 guesses.
  %   - Otherwise, one guess splits the range; the harder half
  %     has ceil(n/2) elements.

  % YOUR CODE HERE

end
